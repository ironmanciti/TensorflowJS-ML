let img;
let poseNet;
let pose;

function setup() {
    createCanvas(640, 480);
    //webcam 연결
    
    //posnet model load
    
    //event listener 연결
    
}

function draw() {
    image(video, 0, 0);
    if (pose) {
        console.log(pose);
        //안경 그리기
        
        //눈알 그리기
        
        //코 그리기
        
        //귀 그리기
       
    }
}
